#include <string>

using namespace std;

extern char ch;
extern string s;
extern int count;
extern const double pi;
int error_number;

extern const char *name;
extern const char *season[];

struct Date;
int day(Date *p);
double sqrt(double num) { return -1; }
template<class T> T abs(T a);

// typedef ?
struct User { string name; bool sex; };
// enum ?
// namespace ?

int main()
{
}
